{
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "Node",
    "declaration": true,
    "declarationDir": "./types",
    "removeComments": false,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "target": "es2017",
    "sourceMap": false,
    "outDir": "./dist",
    "baseUrl": ".",
    // Dentro de este repo, `liteb` ES `lib/`: así el código que genera el CLI
    // (que importa 'liteb', como un consumidor) se TYPECHEQUEA en las pruebas.
    // `liteb` ES `lib/`, y `@/` son los módulos de la demo — el mismo alias que
    // escribe `liteb init`, para que el ejemplo use lo que documentamos.
    "paths": {
      "liteb": ["lib"],
      "liteb/*": ["lib/*"],
      "@/*": ["src/modules/*"]
    },
    "incremental": true,
    "skipLibCheck": true,
    "strictNullChecks": false,
    "noImplicitAny": false,
    "strictBindCallApply": false,
    "forceConsistentCasingInFileNames": false,
    "noFallthroughCasesInSwitch": false,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true
  },
  // ts-node lee esto, y sin ello `npm run dev` muere en el primer import de la
  // demo: `paths` es cosa del compilador y Node no sabe nada de `@/`. Es la
  // misma línea que `liteb init` escribe en el tsconfig que genera.
  "ts-node": { "require": ["tsconfig-paths/register"] },
  "compileOnSave": true,
  "include": ["./src", "jest.config.ts", "./lib"]
}
